name: water_drop_nav_bar
description: Bottom navigation bar. It has unique water drop effect. Indicates with filled icon.
version: 0.0.1
repository: https://github.com/watery-desert/water_drop_nav_bar

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true